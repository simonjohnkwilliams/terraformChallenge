This folder houses all of the terraform files. 
It has been split into Main which will hold all of the items around provisioning which at the moment does as requested
one VPC, one public subnet, two EC2s in the subnet and an ELB to load balance these. 